<% include ../partials/header %>

  <div class="ui four special stackable cards">
    <% series.forEach(function(show){ %>

      <div class="card">
        <div class="blurring dimmable image">
          <div class="ui dimmer">
            <div class="content">
              <div class="center">
                <a href="/series/<%= show.maze_id %>" class="ui inverted button">Apskatīt</a>
              </div>
            </div>
          </div>
          <img src="<%=show.image[0].medium %>">
        </div>
        <div class="content">

          <a href="/series/<%= show.maze_id %>" class="header">
            <%=show.name %>
          </a>

          <div class="meta">
            <a href="http://www.imdb.com/title/<%=show.imdb_link %>">IMDB Links</a>
          </div>
          <div class="description">
            <%-show.description.substr(0,150) %>
            <%if(show.description.length > 150){%>
              ...
            <%}%>

            <p>
                <strong>Žanri: </strong><%= show.genres.join(", ") %>
            </p>
          </div>
        </div>
        <div class="extra content">
          <span class="right floated">
            <% if(show.status === "Running"){ %>
              Episodes iznāk: <%=show.air_days[0] %>
            <% } else { %>
              <strong>Šovs ir BEIDZIES</strong>
            <% } %>
          </span>
        </div>
      </div>

      <% }); %>

</div>


<script>
$('.special.cards .image').dimmer({
on: 'hover'
});
</script>
<% include ../partials/footer %>


<!-- <form action="/results" method="get">
  <input type="text" name="search" placeholder="enter text">
  <button>Submit</button>
</form> -->
